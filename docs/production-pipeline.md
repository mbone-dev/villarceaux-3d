# Pipeline de production

1. Données structurées dans `courses/villarceaux`.
2. Viewer indépendant TypeScript/Babylon dans `packages/viewer`.
3. Démo d'intégration dans `apps/web-demo`.
4. Export statique vers `exports/villarceaux/hole-01/web`.
